function [img,err] = read_raw(fname,nx,ny)% Read in a 8-bit RAW image and outputs data for Matlab IMSHOW% % Created: Prabu Sellappan, 11/14/2014%% usage: img = read_raw(fname,nx,ny)% reads in a 8-bit raw image file, and formats to display properly using imshow% fname = name of the raw image file% nx, ny = number of pixels in the x and y directionsfid = fopen(fname);if fid > 2    err = 0;    imgdata = fread(fid,'*uint8');%   size(imgdata)    fclose(fid);    imgdata = (reshape(imgdata,[nx,ny]))';    img=imgdata;%    img = imgdata(ny:-1:1,:);else    err = 1;    img=[];end